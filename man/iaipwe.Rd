% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iaipwe.R
\name{iaipwe}
\alias{iaipwe}
\title{IAIPWE for K-stage SMARTs with up to 2 treatment options at each stage}
\usage{
iaipwe(df, pi_list, q_list, regime_all, feasible_sets_indicator, t_s, B = NULL)
}
\arguments{
\item{df}{A data frame containing the data. It should include columns for
the treatment assignments, response status, covariates, and outcomes.}

\item{pi_list}{A list of \code{modelObj} objects of length K specifying the
propensity score model at each stage.}

\item{q_list}{A list of \code{modelObj} objects of length K specifying the
outcome regression model at each stage, or \code{NULL} for IPW-only estimation.}

\item{regime_all}{A list of length equal to the number of regimes to be
estimated. Each element is a list with two components: \code{regime}, an
n x K matrix of treatment assignments each individual would receive under
that regime, and \code{regime_ind}, an n x K indicator matrix of whether each
individual was consistent with that regime at each stage.}

\item{feasible_sets_indicator}{A logical value indicating whether feasible
sets are present in the trial design. If \code{TRUE}, some treatments are
assigned deterministically based on response status; if \code{FALSE}, all
stages have random treatment assignment.}

\item{t_s}{A numeric value indicating the time point at which the analysis
should occur.}

\item{B}{A positive integer specifying the number of empirical bootstrap
samples to use for variance estimation, or \code{NULL} (default) to use the
asymptotic sandwich variance estimator.}
}
\value{
A list with the following components:
\describe{
\item{values}{A numeric vector of estimated regime values.}
\item{se}{A numeric vector of standard errors for the regime values.}
\item{covariance}{The estimated L x L covariance matrix of the regime
value estimators.}
\item{params}{A numeric vector of all estimated parameters.}
\item{nus}{A list of estimated stage arrival probabilities, as returned
by \code{\link[=get_nu]{get_nu()}}.}
\item{q_all}{A list of fitted Q-function objects for each regime.}
\item{regime_all}{The input regime list, returned for convenience.}
\item{dfs}{A list of value term matrices for each regime.}
\item{variance_choice}{A character string indicating the variance
estimation method used (\code{"Asymptotic"} or \code{"Bootstrap"}).}
\item{chi_square}{A list with \code{Statistic} (the chi-squared test
statistic for equality of regime values) and \code{p_value}.}
}
}
\description{
The IAIPWE was developed for arbitrary K stage SMART and subsumes both IPWE
and AIPWE. To implement the IPWE, q_list should be NULL and the t_s
should be set to the maximum available time of the outcome observed.
To implement the AIPWE, t_s should be set to the maximum available time of
the outcome observed.
}
\details{
If times are not recorded for the study, "dummy" times can be used with the
analysis time set to be the maximum of the dummy times.
}
