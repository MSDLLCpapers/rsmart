% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime_list_no_trt_resp.R
\name{regime_list_no_trt_resp}
\alias{regime_list_no_trt_resp}
\title{Generate regime lists for a two-stage SMART where responders are not
re-randomized}
\usage{
regime_list_no_trt_resp(
  emb_regimes,
  dat,
  resp_trt = list(r2 = list(0, 0, 0, 0))
)
}
\arguments{
\item{emb_regimes}{A list of numeric vectors, each of length K, specifying
the treatment codes (0 or 1) for non-responders at each stage. For
example, \code{list(c(0,0), c(0,1), c(1,0), c(1,1))} encodes four regimes.}

\item{dat}{A data frame containing the SMART data. Expected to contain
columns \verb{a1, ..., aK} (treatment assignments) and optionally \verb{r1, ..., rK} (response indicators). Typically this is the output from
\code{\link[=gen_no_trt_resp]{gen_no_trt_resp()}}, but any data frame with the required columns may be
used.}

\item{resp_trt}{A named list where each element corresponds to a stage with
deterministic responder treatment (e.g., \code{"r2"}). Each element is a list
of length equal to the number of regimes, specifying the treatment
assignment (0 or 1) for responders at that stage under each regime.
Default is \code{list("r2" = list(0, 0, 0, 0))}.}
}
\value{
A list of length \code{length(emb_regimes)}. Each element is a list with
two components:
\describe{
\item{regime}{An n x K matrix of treatment assignments each individual
would receive if they followed that regime.}
\item{regime_ind}{An n x K indicator matrix (0 or 1) of whether each
individual's observed treatment was consistent with the regime at each
stage.}
}
}
\description{
Constructs the treatment assignment matrices and consistency indicator
matrices for each embedded regime in a two-stage SMART. For stages where
response status determines treatment deterministically, the regime matrix
is updated to reflect the responder treatment.
}
